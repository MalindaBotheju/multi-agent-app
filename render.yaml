services:
  - type: web
    name: multi-agent-app
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn api:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: GROQ_API_KEY
        sync: false   # set the real value in Render's dashboard, not here
